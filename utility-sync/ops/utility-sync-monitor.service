[Unit]
Description=Check Utility Sync service, tunnel, space and backup freshness
After=utility-sync.service

[Service]
Type=oneshot
User=utility-sync
Group=utility-sync
WorkingDirectory=/opt/utility-sync
EnvironmentFile=/etc/utility-sync/environment
ExecStart=/usr/local/bin/uv run --frozen --no-sync utility-sync monitor
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/srv/utility-meter
UMask=0077
